// Visual tests compare rendered pixels, so the test browser has to load the
// same thing a real application would: Tailwind's utilities plus this package's
// tokens, fonts and base layer.
import "./preview.css";
